import SocialMinimiseClient from '@/components/SocialMinimiseClient'

export const metadata = { title: 'Social Minimise — Top Bins' }

export default function SocialMinimisePage() {
  return <SocialMinimiseClient />
}
